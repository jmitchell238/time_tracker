/// Human-visible build identifier shown in Settings so we can tell which
/// build a device is actually running. Bump the suffix on every deploy
/// (see RELEASE.md).
const String kBuildVersion = '1.0.9 — 2026-07-21a';
